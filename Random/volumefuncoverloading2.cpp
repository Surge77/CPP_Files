#include<iostream>
using namespace std;

class volume
{
    public:

    int side,length,breadth,height;
    float radius,he;

    void getdata()
    {
        cout<<"Enter side of cube: ";
        cin>>side;
        cout<<"Enter radius and height of cylinder : ";
        cin>>radius>>height;
        cout<<"Enter length,breadth,height for cuboid: ";
        cin>>length>>breadth>>height;
    }

    void volume(int side)
    void volume(float radius,float he)
    void volume(int length,int breadth,int height)
    
    
};

void::volume(int side)
{
    int R;
    R = side*side*side;
    cout<<"Volume of cube = "<<R<<endl;
}
Void V::Volume(float radius,float he)
{
    float R;
    R = 3.14
}

